<div>
    <div class="title">
        <h2>{{title}}</h2>
        <p>{{description}}</p>
    </div>
    <div class="subtext">
        {{ log "here" }}
        {{ log id }}
        by {{user.username}}, created at {{format_date created_at}}
        <a href="/post/{{id}}"></a>
    </div>

</div>