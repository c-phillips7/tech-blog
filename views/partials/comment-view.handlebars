{{!-- Display of each comment --}}
<div class="comments">
  {{#each this}}
    <section class="comment">
      <div>
        {{user.username}}
      </div>
      <div>
        {{comment_text}}
      </div>
    </section>
  {{/each}}
</div>